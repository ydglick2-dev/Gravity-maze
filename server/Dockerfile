FROM node:20-alpine

WORKDIR /app

COPY package.json ./
RUN npm install --omit=dev

COPY server.js ./

ENV PORT=8080
EXPOSE 8080

# Run unprivileged; the process needs nothing beyond the listening socket.
USER node

CMD ["node", "server.js"]
