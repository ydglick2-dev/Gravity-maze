# Personal side-loading key. Keeping it in the repo is what makes every build
# upgrade-compatible with the last one; losing it would mean uninstalling the
# app to install a new version. Rotate it if this app is ever published.
storeFile=signing/glassify.jks
storePassword=glassify
keyAlias=glassify
keyPassword=glassify
