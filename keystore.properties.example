# Copy to keystore.properties and fill in to sign release builds with your own key.
# Without this file, assembleRelease falls back to the debug keystore so the APK is still
# installable. keystore.properties is gitignored -- never commit a real one.
storeFile=../release.jks
storePassword=CHANGE_ME
keyAlias=kolan
keyPassword=CHANGE_ME
